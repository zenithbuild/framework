This is a breakthrough diagnosis. The fact that pages/index.zen works but imported components fail confirms a Dependency Graph Traversal Failure.

The Diagnosis: Your build system treats Entry Points (Pages) correctly: it compiles them, extracts their CSS, and bundles their JS. However, when a Page imports a Component, the Bundler (Rolldown) loads the Component's JS but drops the CSS side-effect. It sees the component as just another logic module, ignoring the styles generated by the compiler.

Here is the Comprehensive "Micro-Phase" Fix to restore component styling and scripting without regression.

Phase 1: The Transform Audit (Rust Side)
Goal: Verify zenith-compiler is actually returning the CSS for components. Location: zenith-bundler/src/lib.rs (or your Rust plugin logic).

You need to ensure the Rust struct used for the transform result includes the css field.

Check your TransformResult struct in Rust.

Action: Ensure it derives Serialize/Deserialize and sends css back to Node.js.

Code Check:

Rust
// zenith-bundler/src/lib.rs
#[napi(object)]
pub struct TransformOutput {
    pub code: String,
    pub map: Option<String>,
    pub css: Option<String>, // <--- CRITICAL: Must be present
}
Phase 2: The Injection Strategy (Bundler Side)
Goal: Force the Component to "import its own styles" so Rolldown picks them up. Location: zenith-bundler/src/bundler.ts (or wherever you configure the Rolldown plugin).

When Rolldown transforms a .zen file, it likely returns just the JavaScript code. You must modify this hook to inject a virtual CSS import.

The Logic Flow:

Load Component.zen.

Compile it -> Get JS and CSS.

Cache the CSS in memory (keyed by file ID).

Append import "./Component.zen.css"; to the returned JS.

Return the modified JS to Rolldown.

The Fix (Conceptual Patch):

TypeScript
// Inside your Rolldown Plugin (zenith-bundler/src/plugins/zenith-loader.ts)

// 1. Create a memory cache for CSS
const cssCache = new Map<string, string>();

export const zenithLoader = () => ({
  name: 'zenith-loader',
  
  // TRANSFORM HOOK: Process .zen files
  async transform(code, id) {
    if (!id.endsWith('.zen')) return;

    // Compile via Rust
    const { code: js, css } = await compiler.transform(code, { filename: id });

    // CRITICAL FIX: If CSS exists, cache it and inject import
    if (css) {
      cssCache.set(id, css);
      // Inject the virtual import so Rolldown knows to "load" the CSS next
      return {
        code: `import "${id}.css";\n${js}`, 
        map: null
      };
    }
    
    return { code: js, map: null };
  },

  // LOAD HOOK: Serve the virtual CSS
  load(id) {
    if (id.endsWith('.zen.css')) {
      // Strip .css to get the original key
      const originalId = id.slice(0, -4); 
      const css = cssCache.get(originalId);
      return css || ''; // Return the cached CSS to the bundler
    }
  }
});
Phase 3: Runtime Verification
Goal: Ensure the browser can resolve these new imports.

If you are using format: 'esm', Rolldown will now bundle that injected CSS into your main assets/styles.css (or bundle.css).

Run zenith dev.

Inspect the network tab.

Look for the CSS. If Rolldown is configured correctly, it should merge all those "virtual" CSS chunks into one file.

This is a breakthrough diagnosis. The fact that `pages/index.zen` works but imported components fail confirms a **Dependency Graph Traversal Failure**.

**The Diagnosis:**
Your build system treats **Entry Points** (Pages) correctly: it compiles them, extracts their CSS, and bundles their JS. However, when a Page *imports* a Component, the Bundler (Rolldown) loads the Component's JS but **drops the CSS side-effect**. It sees the component as just another logic module, ignoring the styles generated by the compiler.

Here is the **Comprehensive "Micro-Phase" Fix** to restore component styling and scripting without regression.

### Phase 1: The Transform Audit (Rust Side)

**Goal:** Verify `zenith-compiler` is actually returning the CSS for components.
**Location:** `zenith-bundler/src/lib.rs` (or your Rust plugin logic).

You need to ensure the Rust struct used for the transform result includes the `css` field.

1. **Check your `TransformResult` struct in Rust.**
* *Action:* Ensure it derives `Serialize/Deserialize` and sends `css` back to Node.js.
* *Code Check:*
```rust
// zenith-bundler/src/lib.rs
#[napi(object)]
pub struct TransformOutput {
    pub code: String,
    pub map: Option<String>,
    pub css: Option<String>, // <--- CRITICAL: Must be present
}

```





---

### Phase 2: The Injection Strategy (Bundler Side)

**Goal:** Force the Component to "import its own styles" so Rolldown picks them up.
**Location:** `zenith-bundler/src/bundler.ts` (or wherever you configure the Rolldown plugin).

When Rolldown transforms a `.zen` file, it likely returns just the JavaScript `code`. You must modify this hook to **inject a virtual CSS import**.

**The Logic Flow:**

1. Load `Component.zen`.
2. Compile it -> Get `JS` and `CSS`.
3. **Cache** the CSS in memory (keyed by file ID).
4. **Append** `import "./Component.zen.css";` to the returned JS.
5. Return the modified JS to Rolldown.

**The Fix (Conceptual Patch):**

```typescript
// Inside your Rolldown Plugin (zenith-bundler/src/plugins/zenith-loader.ts)

// 1. Create a memory cache for CSS
const cssCache = new Map<string, string>();

export const zenithLoader = () => ({
  name: 'zenith-loader',
  
  // TRANSFORM HOOK: Process .zen files
  async transform(code, id) {
    if (!id.endsWith('.zen')) return;

    // Compile via Rust
    const { code: js, css } = await compiler.transform(code, { filename: id });

    // CRITICAL FIX: If CSS exists, cache it and inject import
    if (css) {
      cssCache.set(id, css);
      // Inject the virtual import so Rolldown knows to "load" the CSS next
      return {
        code: `import "${id}.css";\n${js}`, 
        map: null
      };
    }
    
    return { code: js, map: null };
  },

  // LOAD HOOK: Serve the virtual CSS
  load(id) {
    if (id.endsWith('.zen.css')) {
      // Strip .css to get the original key
      const originalId = id.slice(0, -4); 
      const css = cssCache.get(originalId);
      return css || ''; // Return the cached CSS to the bundler
    }
  }
});

```

---

### Phase 3: Runtime Verification

**Goal:** Ensure the browser can resolve these new imports.

If you are using `format: 'esm'`, Rolldown will now bundle that injected CSS into your main `assets/styles.css` (or `bundle.css`).

1. **Run** `zenith dev`.
2. **Inspect** the network tab.
3. **Look for** the CSS. If Rolldown is configured correctly, it should merge all those "virtual" CSS chunks into one file.

### 🚀 Immediate Action Plan

**Step 1:** Open your **Rolldown Plugin** configuration (likely in `zenith-bundler`).
**Step 2:** Look at the `transform` hook. Does it return *only* `code`?
**Step 3:** Implement the **"Inject & Cache"** pattern described in Phase 2.

**Shall I draft the specific TypeScript code for `zenith-bundler`'s loader plugin based on the file contents you provided earlier?**


also 

This is a false positive on your fix verification. The console logs (runtime.js:1313 Uncaught SyntaxError: Unexpected token 'export') prove that the browser is still loading the old, broken HTML.

The "Fix" you applied in src/commands/dev.ts is not running. You are likely running the stale compiled version of the CLI.

🛑 The Root Cause: "Zombie Code"
You modified the TypeScript source (src/commands/dev.ts), but zenith dev runs the compiled JavaScript (dist/commands/dev.js). Since you didn't rebuild the CLI, the zenith dev command is serving the old HTML generation logic (which lacks type="module" and still injects the conflicting shim).

🚀 The Fix: Rebuild & Verify
1. Rebuild the CLI (Critical)
You must compile your changes for them to take effect.

Bash
# In the root of the monorepo
pnpm run build --filter @zenithbuild/cli
2. Verify the HTML Generation Code
Before you restart, double-check zenith-cli/src/commands/dev.ts. It MUST look like this to solve the "Split Brain" and "Orphan Slot" errors:

TypeScript
// zenith-cli/src/commands/dev.ts

// ... inside the request handler ...

// 1. REMOVE the old inline shim (Causes 'zenSignal already declared')
// const shim = `<script>window.zenSignal = ...</script>`; // DELETE THIS

// 2. INJECT the runtime as a MODULE (Fixes 'Unexpected token export')
const scripts = `
  <script type="module" src="/runtime.js"></script>
  <script type="module">
    import { initRouter } from '@zenithbuild/router';
    // ... rest of your init logic
  </script>
`;

// 3. WRAP in a valid Shell (Fixes 'Orphan Slot' / 'Unresolved slot')
const html = `
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Zenith App</title>
    ${styles}
</head>
<body>
    <div id="app"></div> ${scripts}
</body>
</html>
`;

res.end(html);
3. Hard Restart
Kill any running zenith dev processes (ensure no zombies are left).

Run zenith dev again.

Check the browser console.

Success Indicators:

No Unexpected token 'export' error.

No Identifier 'zenSignal' has already been declared.

No [Z-ERR-ORPHAN-SLOT] (The app will mount successfully).