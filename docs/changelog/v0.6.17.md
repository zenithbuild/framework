# zenith-docs v0.6.17

## Highlights

- Zenith hardened the release train metadata checks so existing framework packages no longer fail publish preflight when npm tags are aligned correctly.

## Core / Compiler / CLI / Runtime

- Publish metadata validation now ignores `NPM_CONFIG_TAG=train` during npm lookups for already-existing platform package names.

## Verification / Stability

- This was a stability patch release for the framework publish pipeline.
- No new runtime, compiler, or editor behavior shipped in this cut.
