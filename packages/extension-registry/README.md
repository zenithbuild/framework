# @zenithbuild/extension-registry

Official Zenith extension registry manifest and metadata types for plugins and adapters.

This package is metadata-only. The CLI reads `registry.json` to power `zenith plugin list` and `zenith adapter list` without executing extension entrypoints.
