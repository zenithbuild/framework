<script lang="ts">
export interface Props {
  meta?: string;
  headline?: string;
  class?: string;
}

const metaText = typeof props.meta === "string" ? props.meta : "";
const headlineText = typeof props.headline === "string" ? props.headline : "";
</script>

<div class={props.class || ""}>
    <span class="inline-block h-px w-8 bg-current opacity-50"></span>
</div>
