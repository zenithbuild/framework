# Benchmark Results

Raw benchmark runs are written under timestamped subdirectories:

- `results/<run-id>/environment.json`
- `results/<run-id>/cold-build.json`
- `results/<run-id>/dev-startup.json`
- runner-specific logs and startup profile captures

These artifacts are for local validation and later report generation. They are not public benchmark claims on their own.
